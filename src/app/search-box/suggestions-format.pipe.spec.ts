import { SuggestionsFormatPipe } from './suggestions-format.pipe';

describe('SuggestionsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SuggestionsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
